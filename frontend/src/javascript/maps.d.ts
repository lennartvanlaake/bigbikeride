import * as L from "leaflet";
import type { Coordinates } from "../../../types/types";
export default function createMap(container: HTMLElement, coords: Coordinates, zoom: number): L.Map;
//# sourceMappingURL=maps.d.ts.map